# ---- data: never in git ----------------------------------------------------
# raw captures (GBs, private)
data/
Data/
# generated databases — rebuild with `elidedb` ingest or python/elidedb/migrate.py
lake/
store/
store_oxford/
stores/

# ---- separate research project, not part of the database product ----------
# FDNN_BrainModel is the neuron/pruning research this repo's context tower
# borrows from (see docs/CONTEXT.md). It carries its own 426 MB virtualenv.
FDNN_BrainModel/

# ---- build artifacts -------------------------------------------------------
build/
*.o
*.a
*.so
*.dylib
__pycache__/
*.pyc
*.egg-info/
dist/
.eggs/

# ---- run/scratch output ----------------------------------------------------
bench_results.jsonl
notebooks/.ipynb_checkpoints/
.ipynb_checkpoints/
*.log
nohup.out

# ---- OS / editor -----------------------------------------------------------
.DS_Store
.vscode/
.idea/

# ---- model caches (downloaded on first use) --------------------------------
.cache/

# ---- internal working notes (not part of the product) ----------------------
CLAUDE.md
docs/memory/
docs/logs/
myenv
# ---- evaluation ground truth: derived from a dataset, not part of the DB ---
# eval/ holds label files (e.g. Bridge task strings) used ONLY by benchmark
# scripts. They are deliberately outside the store so the query path cannot
# reach them; they are also not the product, so they are not shipped.
eval/
