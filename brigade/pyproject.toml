[project]
name = "brigade"
version = "0.1.0"
description = "An autonomous kitchen agent whose memory is ElideDB on CockroachDB"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["brigade*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
