# CPU wheels only. The GPU build of torch is 2.5 GB of CUDA this demo never
# executes: nothing in the request path does a forward pass over video, and the
# one model that does run is a text tower on two threads.
--extra-index-url https://download.pytorch.org/whl/cpu

fastapi==0.141.1
uvicorn==0.52.3
psycopg2-binary==2.9.10
boto3==1.43.73
numpy==2.2.6
torch==2.9.1
transformers==4.57.6
