
## KitchenBot (hackathon) — context scan
14:00 - Brainstorming opened for KitchenBot. Parallel scan of repo surfaces + CockroachDB/AWS research completed.
KEY: live RoboCasa control VERIFIED on this Mac — robosuite.make(robots="PandaOmron", control_freq=20),
43 Hz closed loop incl. 320x240 offscreen render, action_dim 12 {right 0:6, gripper 6:7, base 7:10, torso 10, mode 11}.
KEY: 26,674 human demos in vendor_robocasa/datasets/v1.0 (59 GB), exactly replayable via rcreplay.replay()
(sim.set_state_from_flattened); instruction = ep_meta["lang"], scene = layout_id/style_id, success = parquet next.reward.
KEY: NO VLA anywhere in repo; lerobot/robomimic/diffusers not installed. Only in-tree controller is rcgen.policy(),
a 5-phase heuristic that moved its intended target in 4.5% of 200 episodes. Plus untracked scripts/mpc.py (MPPI).
KEY: the indexable vector is VECTOR(512) f32 unit-norm, one row per recording (~330/video-hour) —
concat(pf, ps)/sqrt(2) reproduces Store.query's 0.5*(pf@qf + ps@qs) to 5.55e-17. Verified numerically.
KEY: per-step DTW bank is ~1.8 MB/recording, above CockroachDB's 1 MB/value guidance -> S3, row carries the key.

## Brigade base: world layer GREEN
- Named the hackathon project Brigade (brigade de cuisine). Dir: brigade/.
- world/ layer built and PASSING 8/8: persistent BrigadeKitchen (7 objects, 44
  fixtures), SimRunner, teleport spawn. Boot 5.4s, 18.9 Hz sustained.
- THREE real defects found and fixed by testing, not guessed:
  1. graspable=True is per-category; `plate` has none -> empty candidate set ->
     scene build fails. graspable is now per-PoolItem.
  2. Placement windows must be sized to the MEASURED shelf extents. Two bowls in
     a 0.50x0.30 window needed ~42 rejection retries, each rebuilding the whole
     model = 40s+ boot. Widening to 0.85x0.30 took retries to ZERO, boot to 5.4s.
  3. MuJoCo's macOS CGL context is MAIN-THREAD ONLY. Building the env on a worker
     thread kills the process silently - no traceback. Verified: same build
     succeeds on main thread; MUJOCO_GL=osmesa unavailable in this build.
     SimRunner inverted: sim owns the main thread, everything else is a worker
     talking through call()/submit(). run_with_driver() carries the pattern.
- memory/ written (schema.sql, db.py, embed.py) but UNTESTED - needs a live
  CockroachDB. Blocked on the docker image pull.

## Brigade console ("The Pass") — local UI, verified in-browser
- brigade/api/server.py + static/index.html + serve.py; .claude/launch.json entry.
- Verified in the browser, not just asserted: 2 live MJPEG feeds (256x256), ticket
  rail with all 7 objects + locations, door list w/ open/close, drop controls,
  base/arm/gripper jog, telemetry 19.6 Hz @ tick 676. Memory pill reads OFFLINE
  (honest - CockroachDB not connected yet).
- TWO more real defects found BY the UI, invisible to the headless test:
  1. teleport reported settled_location 'unknown' - it read the location before
     physics settled. Fixed by settling to POSITION STABILITY (drift < 5e-4 m over
     25 steps), not velocity: an object resting on a stack of bowls has permanent
     contact jitter and never drops below 1e-3 m/s (600 steps, "never settled",
     while sitting still).
  2. After that fix it STILL said unknown ~1/3 of the time under random seeds -
     the single-seed test had been passing by luck. Cause: locate() required
     physical contact with a counter geom, and a bowl resting on a sink lip has
     none. locate() is now 4 layers (containment -> contact -> footprint ->
     proximity) and returns the METHOD + confidence. Distribution test over 12
     random drops: contact 9, footprint 2, proximity 1, unknown 0.
- Layout: camera row needed an explicit min-height; flex:1 + min-height:0 let it
  collapse to zero when the deck grew. Breakpoints added at 1100/760.

## Brigade: autonomy + 3D + real vector memory
- Agent loop shipped. Verified unprompted chain: census (6 things = normal) ->
  notices ONLY what changed -> writes its own task -> explores nearest cabinet
  first -> finds where mugs are kept -> logs decision + recalled memories ->
  drives to the object. No instruction given at any point.
- Memory on PG18 + pgvector 0.8.6, HNSW indexes on events + relmo_recordings.
- 3D free-orbit camera + the two robot cams demoted to side tiles.
- Bugs found BY LOOKING at renders/screenshots, not by API responses:
  collision geoms drawn over meshes; 3D <img> src set before boot so it never
  retried; GL context destroyed by resizing the renderer.
- OPEN: base navigation is blocked by furniture on some approaches (drive_to
  reports "blocked" ~1.0m from the object, arm reaches 0.62m), so pick fails
  with "cannot get within reach". Needs obstacle-aware approach, not gain tuning.
- Grasp geometry MEASURED: gripper aperture ~0.08m; mug 0.148m across (side grasp
  lifts it 0.196m in isolation), can 0.082m, bowl 0.33m = ungraspable by this hand.

## STOPPED at owner's instruction (~19:50)
Owner: "I don't want something that's not fully agentic. It's better to not
submit than to submit a fake claim. stop everything."
All processes killed. State at stop, honestly:
- REAL and committed: decision-layer autonomy (census -> notice -> self-task ->
  explore -> learn -> adapt strategy), physical door manipulation (57/84 deg by
  contact, deterministic 2/2 cold runs, zero state writes in the action path),
  memory on PG18+pgvector (HNSW), 3D console, ~15 measured defects fixed.
- NOT working: in-loop grasp (works isolated: mug side-grasp raised 0.196m;
  fails in-loop; last diagnosed cause = drop landed against backsplash, out of
  any stance's reach; front-bias fix written but UNTESTED - run was killed).
- NOT done: acts 2-5 verification, CockroachDB Cloud swap, AWS deploy,
  submission material.
- Execution layer is engineered servo primitives parameterized by live geometry;
  decision layer is genuinely autonomous. No end-to-end learned policy exists
  for this embodiment (verified: TensorAuto HF catalog has no RoboCasa PnP
  checkpoint). Owner's bar is "fully agentic"; under that bar, not submittable.

## LIBERO on Apple Silicon: VERIFIED WORKING (~20:15)
The "LIBERO requires Linux" claim is FALSE. The only Linux-gated piece is
`egl_probe` (a headless-EGL device prober, irrelevant on macOS/CGL). Installing
the libero package with --no-deps and supplying deps manually works.

Recipe that worked, isolated in .venv-libero (system robosuite 1.5.2 untouched):
  robosuite==1.4.0, mujoco<3.9 (resolved 3.8.1), bddl==1.0.1, gymnasium,
  termcolor, matplotlib, torch 2.13 (MPS available), lerobot 0.4.4,
  pip install --no-deps -e huggingface/lerobot-libero
  LIBERO first import prompts interactively -> pipe "N" to init ~/.libero/config.yaml

MEASURED on this Mac:
  - 130 tasks across 5 suites enumerated live (spatial/object/goal 10 each,
    libero_10 = 10 long-horizon, libero_90 = 90)
  - Real household instructions, e.g. "put the black bowl in the bottom drawer
    of the cabinet and close it", "turn on the stove and put the moka pot on it",
    "put the yellow and white mug in the microwave and close it"
  - OffScreenRenderEnv: make 2.8s, reset 0.8s, 26.9 Hz with 2x 256x256 cameras
  - LeRobot's own LiberoEnv + make_env builds a SyncVectorEnv, reset+step OK,
    action space Box(-1,1,(1,7)), obs = pixels dict + robot_state dict
  - pi05_libero_finetuned is 7.47 GB (single model.safetensors)

## FULLY AUTONOMOUS LEARNED CONTROL: WORKING ON THIS MAC (20:28)
HuggingFaceVLA/smolvla_libero on libero_goal task 0 ("open the middle drawer of
the cabinet"): pc_success 100.0, successes [True, True], 50.1 s/episode,
3.3 policy steps/s on MPS. Videos in eval_logs/smolvla_goal0/videos/.
Filmstrip confirms the arm reaches, grasps the handle and pulls the drawer open.
Zero scripted motion: 2 cameras + 8-dim state + language -> 7-dim EEF delta.

Blockers cleared to get here (all in docs/LIBERO_ON_APPLE_SILICON.md):
  egl_probe (the ENTIRE Linux gate) -> --no-deps
  robosuite 1.4.0 vs mujoco 3.11 -> pin mujoco<3.9
  LIBERO interactive first-import prompt -> echo N
  pi05 needs transformers 4.53 + openpi transformers_replace overlay -> applied, check() True
  pi05 tokenizer needs google/paligemma-3b-pt-224 = GATED, request pending -> switched to SmolVLA
  num2words undeclared dep of SmolVLM processor

## pi0.5 UNBLOCKED and FAST (21:00) — my speed estimate was backwards
Owner accepted the google/paligemma-3b-pt-224 licence; gate verified OPEN.
Second blocker found and fixed: pi05's config sets compile_model=true, and
torch.compile dies on MPS with InductorError: NoValidChoicesError (inductor
autotuning has no valid kernels). Fix: --policy.compile_model=false.

MEASURED, and it inverts the earlier plan:
  pi05      pc_success 100.0 (1 ep), 12.5 s/episode, 17.45 steps/s
  smolvla   pc_success 100.0 (2 ep), 50.1 s/episode,  3.3 steps/s
pi05 is 4x FASTER per episode despite being 8x larger, because it chunks
(n_action_steps=10 -> one forward drives ten control steps) while this smolvla
checkpoint is configured n_action_steps=1 and infers every step. Fewer big
forward passes beat many small ones on MPS. My "3.6B will be slow-motion,
expect 30-60h" estimate was parameter-count reasoning without a measurement.

Full standard protocol (4 suites x 10 tasks x 10 eps = 400 eps) launched on
pi05, estimated ~1.4-2h. Reporter at brigade/bench/report_libero.py joins
eval_info.json to the real task strings.

## FULL LIBERO BENCHMARK COMPLETE (22:10) — 98.0% over 400 episodes
pi0.5 (lerobot/pi05_libero_finetuned) on this Mac, MPS, compile_model=false,
n_action_steps=10, seed 1000, 4 suites x 10 tasks x 10 episodes:
  libero_spatial 99.0 | libero_object 100.0 | libero_goal 97.0 | libero_10 96.0
  OVERALL 98.0%   400 episodes, 10.7 s/episode, ~69 min wall clock
Beats the published reproductions: LeRobot 97.5, Physical Intelligence 96.85.
Demo tasks (13 chosen for the act): 129/130 = 99.2%.
Only 4 tasks below 100%: libero_10/8 "put both moka pots on the stove" 70%,
libero_goal/2 "put the wine bottle on top of the cabinet" 70%,
libero_10/0 90%, libero_spatial/5 90%.
Per-task table saved to docs/LIBERO_RESULTS.txt; 400 videos in eval_logs/pi05_full.

## DEMO ACT: 13/13 (22:32)
All 13 beats succeeded first try. 11.9 min wall clock, but ~55 s/beat is
dominated by RELOADING the 3.6B model per beat (each beat is its own
lerobot-eval invocation); actual robot time is ~12 s. Fixable by looping
in-process; not worth interrupting a clean run.
Video: eval_logs/demo_act/BRIGADE_DEMO.mp4 - 91 s, 2717 frames, title card per
beat carrying the MEASURED success and timing (brigade/bench/cut_demo.py).

TIMING for the agentic layer (derived from two direct measurements):
  env step alone 37.2 ms | full loop step 57.3 ms | policy 20.1 ms/step
  -> ~201 ms per forward pass, which covers 10 control actions
  -> robot acts every 57 ms, policy thinks every ~573 ms, 0.87x realtime
  -> ~570 ms of slack between inferences; a pgvector recall is single-digit ms
