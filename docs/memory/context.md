# Context

- Dataset layout (data/20260403_181625): 4 sensors (108/122/184/227), each with video/ (MJPEG 4K@30fps AVI, ~960 frames per ~500MB segment, cameras "0" and "2"), time/ (450-frame JSON bundles: python_timestamp sec, global_timestamp ticks, frame_id global counter, file_index -> AVI segment), audio/ (16ch 48kHz s16 WAV, 5s each, named by python epoch sec), logs/. (2026-07-17)
- global_timestamp is the shared cross-sensor clock, ~1200.2 Hz ticks (empirically consistent across all 4 sensors); python clocks are wildly unsynced (Sensor 122 differs by ~2.8 years). Canonical timeline = fitted global clock; per-stream clock_offset_ns recovers local wall clocks. (2026-07-17)
- MJPEG is intra-only: every frame is a keyframe, GOP = 1 frame, so SFI byte-range decode is frame-exact. (2026-07-17)
- Time JSONs may cover AVI segments that were not copied (more JSONs than AVIs); last AVI per camera may be truncated. Ingest joins per file_index by order and takes min(count), warns on mismatch. (2026-07-17)
- Toolchain: Apple clang 17, cmake+ninja via homebrew, FFmpeg headers at /opt/homebrew, python 3.11.6 (pyenv), no pybind11 installed yet. 400GB disk free. (2026-07-17)
- User measures the database by the classic yardstick: compression without quality loss IS the point of a database; store size vs raw size is a headline number now (2026-07-28)
- CPU serving of the current Python engine is "pathetically slow" in the user's words; speed of the core engine is the second yardstick, and Rust is the chosen answer (2026-07-28)
- Local pyenv 3.11.6 has torch 2.9.1 + (since 2026-07-28) open_clip_torch; PE text vectors cache in-memory only, so any bench run loads text towers (2026-07-28)
- Terminology correction from the user (2026-07-28): stop calling them "semantic channels". The product is CONTEXTUAL understanding (what happened: opened vs closed, spoon onto cloth); semantic similarity is the commodity half. The ablation confirms it - the contextual channels carry the result.
- DEFINITIVE purpose (user, 2026-08-08): ElideDB = "give a video, ask when something like this happened". Example-query first; description only if ever asked. It is BOTH a database powered by a world-model memory AND a memory layer for a world model. Local product first, robotics customers, but works on any data. Four uses: (1) retrieval from long old memory, (2) bulk heavy video processing, (3) retraining robots on retrieved failure cases, (4) decompose a human demo -> retrieve matching robot sub-memories -> compose to perform the task.
- RULES corrected (2026-08-08): labels/ground truth ALLOWED IN EVAL ONLY. Read path, write path, and training are all label-free — no fixed axes, no channels, no "stack/push" anywhere in the system; everything lives in latent space and similarity is latent matching. Training the model myself is allowed, including on MuJoCo sim data I generate; sim ground truth is for MEASURING extraction quality, never for training. The clip (temporal) is the unit, not the frame.
- sim_chains meta.json = 975 labelled events (450 pick / 250 stack / 200 place / 50 unstack / 25 push), exact t0/t1, colors, success flags, 6 templates x 25 eps. Eval-only instrument. I ignored it all day while hand-verifying 4 spans.
- Raw-pixel baseline (32x32 thumbnails): sev-rho 0.553 / struct-rho 0.390 — BEATS frozen ViT-S heads (0.538/0.449 best). Any new latent must clear the pixel floor or it is noise.
- Owner: never describe entities into kind-spaces; lookalikes (pen vs cosmetic) must stay separate because they are separate INSTANCES - identity by continuity only (2026-08-10)
- Owner: V1 may assume occlusion is avoided at capture; do not solve occlusion now (2026-08-10)
- Owner pushback pattern to respect: single-axis shortcuts are rejected; every axis needs the factored structure first, so build the structure, not the axis (2026-08-10)
- OWNER 2026-08-10: dataset trust burned. RULES: (1) a failed episode is
  NOISE - success-gated generation only, verify before use; (2) don't
  overpopulate scenes - one object at a time for primitives; (3) order of
  work: "primitive action" dataset (5 prims x 3 arms, episode-verified)
  FIRST, then chains (same+different arms); (4) record the arm in meta;
  (5) report after the dataset is fixed, not during.
- HOLD (2026-08-11): after ftrack_v1 finishes, STOP and wait for the owner's signal. Do not re-track caches with a promoted tracker, do not restart WM training, do not start any new job. Report the final gate + trackval numbers and hold. (2026-08-11)
- AUTONOMOUS MODE (2026-08-12): owner unavailable, granted full autonomy to ingest + validate + train. Driver is native/relmo/pipeline.py — a state machine over ARTIFACTS (not a script): generate rcasa -> track -> G0 learnability gate -> train. Idempotent, halts on gate failure rather than working around it. Cron f6b5dd73 ticks it every 25 min. HOLD from 2026-08-11 is SUPERSEDED by this grant. (2026-08-12)
- CRASH (2026-08-12): kernel panic, dart-ave0 DART PTE-invalid on write — a DMA fault in the Apple Video Encoder path, i.e. HARDWARE/driver level, not a Python error. Our pipeline is video-heavy (ffmpeg encode per episode in rcgen/movi/physgen, ffmpeg decode per episode in tracks2). Nothing was lost: all 3 checkpoints load clean, all 4 corpora intact (600/301/1616/2000), training auto-resumed from ckpt 5000. If it recurs, suspect concurrent ffmpeg + MPS load; mitigation is to serialise video work away from training rather than run both. (2026-08-12)
- NEW PROJECT (2026-08-16): KitchenBot — CockroachDB x AWS agentic-memory hackathon entry, new top-level directory. Kitchen robot in RoboCasa/MuJoCo with CockroachDB as the persistent memory layer and RelMo/ElideDB as retrieval. Owner's target demo: drop a bowl -> robot stacks it in the cabinet; "move the bowls" -> it does and REMEMBERS where they went; "make me tea" -> it recalls where utensils are from memory plus VLA vision. Owner's stated priority: "a really good VLA that can run on this Mac". Hackathon requires >=2 CockroachDB tools (managed MCP server, distributed vector index, ccloud CLI, agent-skills repo) and >=1 AWS service; judged on agentic memory design / technical implementation / real-world impact / production readiness / creativity. (2026-08-16)
- Owner instruction (2026-08-16): do not spin up new subagents/workflows without being asked.
- KitchenBot scope settled by owner (2026-08-16): VLA is NOT a must. Requirements: robot looks at things (real perception), acts autonomously, follows instructions when given, and — the main part — retrieves events from memory. One big kitchen, different tasks, realtime. Also in scope: text->memory lookup for RelMo does not work yet; quickly training a domain text tower inside the hackathon directory is good enough.
- KitchenBot HARD constraints from owner (2026-08-16): $0 budget — AWS free-tier services only, CockroachDB free tier only, NO paid Bedrock. Deadline 1 DAY. ~4000 participants, real-world use case is a must. Owner verdict on ElideDB/RelMo: "most disgusting broken system... works only barely" — do NOT make RelMo retrieval load-bearing; memory must be built on components that reliably work. Wants a sure one-shot winnable build, not a fragile demo.
- KitchenBot (2026-08-16, owner): RelMo MUST be used and MARKETED as a product despite known weakness — the hackathon is its launch vehicle. Both RelMo and CockroachDB must be central (not garnish), all hackathon requirements met, NO faking. The five autonomous acts approved as the demo shape.
- Brigade HARD RULE from owner (2026-08-16 evening): NO state manipulation in the robot's action path. Doors opened by fixture.open_door() qpos writes "look fake / open randomly" — every door must move because the gripper physically pulled/pushed it. "I want an agent operating the robot, not the state." Reads (poses for targeting, door state for verification) are fine; writes are not. God-hand teleports for HUMAN pokes stay, but must not flip doors — objects may materialize inside closed cabinets instead.
- PIVOT (2026-08-16 ~20:00): kitchen system abandoned per owner. New requirement: ANY robot simulation with FULL autonomous control = learned end-to-end policy in closed loop, released + tested by others, no hand-rolled skills. Surveyed: LeRobot ACT/aloha-sim 83% published (51.7M, verified card), diffusion PushT, robomimic zoo (Lift/Can ~100% but mujoco-py era = Apple Silicon pain), lerobot pi0_libero_finetuned via lerobot-eval, lerobot/being_h05_robocasa 3B exists, OpenTau (navigate/close only), mujoco_playground = no shipped checkpoints, GR00T = NVIDIA only.
